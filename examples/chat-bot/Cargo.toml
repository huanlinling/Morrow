[package]
name = "chat-bot"
version = "1.0.0"
edition = "2021"
description = "Morrow chat bot example — responds to messages"

[lib]
crate-type = ["cdylib"]

[dependencies]
morrow = { path = "../../sdk-rs" }
