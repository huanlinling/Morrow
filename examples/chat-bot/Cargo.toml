[package]
name = "chat-bot"
version = "0.1.0"
edition = "2021"
description = "Morrow chat bot example — responds to messages"

[lib]
crate-type = ["cdylib"]

[dependencies]
log = "0.4.33"
morrow = { path = "../../sdk-rs" }
