[package]
name = "motd"
version = "0.1.0"
edition = "2021"
description = "Morrow example — welcome message from config.toml"

[lib]
crate-type = ["cdylib"]

[dependencies]
morrow = { path = "../../sdk-rs" }
serde = { version = "1", features = ["derive"] }
