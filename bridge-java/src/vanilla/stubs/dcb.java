/** Stub of 1.20.1's obfuscated BlockState. getBlock() is really declared
 *  on the superclass (dca$a) — declaring it here keeps javac happy and
 *  resolves identically at runtime (invokevirtual walks up). */
public class dcb {
    public cpn b() { return null; } // getBlock()
}
