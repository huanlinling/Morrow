{
    "schemaVersion": 1,
    "id": "morrow",
    "version": "${version}",
    "name": "Morrow",
    "description": "Native mod loader — Rust runtime via Panama FFI",
    "license": "MIT",
    "environment": "*",
    "mixins": ["morrow.mixins.json"]
}
