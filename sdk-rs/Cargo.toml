[package]
name = "morrow"
version = "1.0.0"
edition = "2021"
description = "Morrow SDK — Rust crate for building Minecraft mods"

[dependencies]
morrow-macros = { path = "morrow-macros" }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
